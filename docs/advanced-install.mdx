---
title: "Advanced Installation"
description: "Install, update, and remove NVIDIA skills with control over agent, scope, and non-interactive workflows."
---

Use this guide when you need more control than the root README quickstart: non-interactive installs, specific agents, global vs project scope, updates, removals, or a manual fallback.

## Requirements

- Node.js and npm available on your PATH. The examples use `npx`, which ships with npm. Your Node.js version must satisfy the `skills` package's published `engines` requirement.
- A current `skills` CLI. Running a command via `npx skills@latest` is the quickest way to rule out stale CLI behavior.
- Access to `https://github.com/NVIDIA/skills`.
- An AI agent that supports Agent Skills, such as Claude Code, Codex, Cursor, OpenCode, Windsurf, Gemini CLI, or another compatible agent.

## List Skills Before Installing

```bash
npx skills add nvidia/skills --list
```

Use this when you want to inspect the catalog before selecting a skill.

## Install One Skill

```bash
npx skills add nvidia/skills --skill cuopt-numerical-optimization-api
```

Use the skill name from the `name:` field in the skill's `SKILL.md`. In this catalog, names usually match the leaf folder under `skills/<product>/`.

## Install to a Specific Agent

```bash
npx skills add nvidia/skills --skill cuopt-numerical-optimization-api --agent codex
npx skills add nvidia/skills --skill cuopt-numerical-optimization-api --agent claude-code
npx skills add nvidia/skills --skill cuopt-numerical-optimization-api --agent cursor
```

You can pass `--agent` more than once:

```bash
npx skills add nvidia/skills \
  --skill cuopt-numerical-optimization-api \
  --agent codex \
  --agent claude-code
```

## Choose Project or Global Scope

Project installs are the default. They place the skill under the current project's agent-specific skills directory, using copies or links as appropriate for the selected agents, so the skill can travel with that project.

```bash
npx skills add nvidia/skills --skill cuopt-numerical-optimization-api
```

Global installs make the skill available across projects for the selected agent:

```bash
npx skills add nvidia/skills \
  --skill cuopt-numerical-optimization-api \
  --agent codex \
  --global
```

## Non-Interactive Install

Use `--yes` when scripting setup or bootstrapping a development environment:

```bash
npx skills add nvidia/skills \
  --skill cuopt-numerical-optimization-api \
  --agent codex \
  --global \
  --yes
```

## Update or Remove Installed Skills

```bash
# See installed skills
npx skills list

# See global skills
npx skills list --global

# Update installed skills (choose a scope interactively)
npx skills update

# Remove a skill
npx skills remove cuopt-numerical-optimization-api
```

`npx skills update --project` and `npx skills update --global` select a scope non-interactively. Updates are applied immediately; the CLI does not provide a dry-run preview command.

## Manual Fallback

Prefer `npx skills add` because it handles agent directories, global vs project scope, and updates. If you cannot use Node.js or npm, you can still copy a skill directory manually:

1. Open the product folder under [`skills/`](../skills/).
2. Copy the specific skill directory containing `SKILL.md`.
3. Place it in your agent's skills directory.

Common global locations are:

| Agent | Global skills directory |
|-------|-------------------------|
| Claude Code | `~/.claude/skills/` |
| Codex | `~/.codex/skills/` |
| Cursor | `~/.cursor/skills/` |

Manual installs do not participate in `npx skills update`, so use them only as a fallback.

## Troubleshooting

### Installed skills don't appear in your agent

1. **Rule out stale CLI behavior.** Run `npx skills --version`, then retry the install with `npx skills@latest add ...` if needed. If npm reports an engine mismatch, update Node.js before retrying.
2. **Refresh the correct install type.** Claude Code live-reloads standalone skills inside existing skill directories. Restart it if the top-level skills directory did not exist when the session began. For skills supplied by a marketplace plugin, run `/reload-plugins` or restart Claude Code.
3. **Check where you installed it.** The install summary reports the exact target. A standalone skill appears directly in `/skills`; a plugin-provided skill is namespaced as `<plugin-name>:<skill-name>`. MCP tools appear in `/mcp` only when the plugin also declares an MCP server.

### Verify your agent can see an installed skill

After installing `<skill-name>`, confirm it landed where your agent looks:

| Agent | Check |
|-------|-------|
| Claude Code | `.claude/skills/<skill-name>/SKILL.md` resolves (project) or `~/.claude/skills/<skill-name>/SKILL.md` (global); the skill also appears in `/skills`. |
| Codex | `.agents/skills/<skill-name>/SKILL.md` resolves — Codex reads `.agents/skills/` directly, so no per-agent link is needed. |
| Cursor | `.agents/skills/<skill-name>/SKILL.md` resolves (project) or `~/.cursor/skills/<skill-name>/SKILL.md` (global). |

For a machine-readable check, run `npx skills list --json` for project installs or add `--global` for global installs. You can also filter either command with `--agent <agent-name>`.
